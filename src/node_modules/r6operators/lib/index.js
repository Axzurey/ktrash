"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
// eslint-disable-next-line import/no-named-as-default
var operator_1 = __importDefault(require("./modules/operator"));
var operators_json_1 = __importDefault(require("./operators.json"));
// create new object with the keys of the JSON object
var output = Object.keys(operators_json_1.default)
    // map the Operator() object to each item
    .map(function (op) { return operator_1.default(op, operators_json_1.default[op]); })
    // reassign the item with the id of the operator
    // eslint-disable-next-line unicorn/no-reduce
    .reduce(function (object, op) {
    object[op.id] = op;
    return object;
}, {});
module.exports = output;
//# sourceMappingURL=index.js.map